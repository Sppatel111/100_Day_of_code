--1. Select the detail of the employee whose name start with P.

select * from empdetails where empname like 'P%';

--2. How many permanent candidate take salary more than 5000.

 select count(*) from empsalary where ispermanent='Yes' and salary > 5000;

--3. Select the detail of employee whose emailId is in gmail.

select * from empdetails where emailid like '%gmail%';

--4. Select the details of the employee who work either for department E-104 or E-102.

 select * from empdetails where department in('E-102','E-104');

--5. What is the department name for DeptID E-102?

select deptname  from empdept where deptid='E-102';

--6. What is total salary that is paid to permanent employees?

select sum(salary) from empsalary where ispermanent='Yes';

--7. List name of all employees whose name ends with a.

select empname from empdetails where empname like '%a';

--8. List the number of department of employees in each project.

 select projectid, count(empid) from empproject group by  projectid;

--9. How many project started in year 2010.

 select count(*) from empproject where startyear='2010';

--10. How many project started and finished in the same year.

select count(*) from empproject where startyear = endyear;

--11. select the name of the employee whose name's 3rd charactor is 'h'.

select empname  from empdetails where empname like '__h%';

--12. Select the department name of the company which is assigned to the employee whose employee id is grater 103.

select deptname from empdept where deptid= any(select department from empdetails where empid >103);

--13. Select the name of the employee who is working under Abhishek.

 select empname from empdetails where empheadid=any(select empid from empdetails where empname='Abhishek');

--14. Select the name of the employee who is department head of HR.
----------
wrong:
select empname from empdetails where department=any(select deptid from empdept where deptname='HR');

correct:
select empname from empdetails where empid=any(select depthead from empdept where deptname='HR');
select e.empname from empdept as d inner join empdetails as e on d.depthead == e.empid where deptname='HR';

--15. Select the name of the employee head who is permanent.
----------
Wrong:
 select empname from empdetails where empheadid =any(select empid from empsalary where ispermanent='Yes');
 correct:
 select empname from empdetails where empid =any(select empid from empsalary where empid in(select empheadid from empdetails) and  ispermanent='Yes');
select e.empname from empsalary as s join empdetails as e on s.empid=e.empid where s.empid in(select empheadid from empdetails) and ispermanent='Yes';

--16. Select the name and email of the Dept Head who is not Permanent.
----------
Wrong:
select EmpName,EmailId from EmpDetails where EmpId=ANY(select EmpId from EmpSalary where IsPermanent='No');
correct:
select empname,emailid from empdetails where empid=any(select d.depthead from empdept as d join empsalary as s on s.empid=d.depthead where s.IsPermanent = 'No');
select e.empname,e.emailid from empdept as d join empdetails as e on d.depthead=e.empid join empsalary as s on d.depthead=s.empid where s.IsPermanent ='No';

--17. Select the employee whose department off is Monday

select * from empdetails where department= any(select deptid from empdept where dept_off='Monday');

--18. Select the indian clinets details.

select * from clienttable where cid=any(select cid from country where cname='India');

--19. Select the details of all employee working in development department.

 select * from empdetails where department= any(select deptid from empdept where deptname='Development');

--20. Select the client name, who has worked on one project at least for one year and half year.
-----------
select clientname from clienttable where clientid=any(select clientid from empproject where  endyear is  not null and (endyear-startyear)>=1.5 );
select c.clientname from empproject as p join clienttable as c on p.clientid=c.clientid where endyear is  not null and (endyear-startyear)>=1.5 ;